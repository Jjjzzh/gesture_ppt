# 项目特点与亮点

---

## 一、核心技术亮点

### 1. 实时手部关键点检测
- 基于 **MediaPipe Hand Landmarker**，识别手部 21 个关键点
- 帧率稳定 25-30 FPS，手势响应延迟 < 0.2 秒
- 通过指尖与关节的相对位置关系，精确判断每根手指的伸直/弯曲状态

### 2. 智能手势识别
- **五指张开** → 开始放映：检测 3+ 手指伸展 + 拇指外展
- **比 1** → 下一页：仅食指伸直，其余三指弯曲
- **比 2** → 上一页：食指+中指伸直，无名指小指弯曲
- 手势需保持连续 N 帧确认，避免瞬时误判

### 3. 景深虚化效果
- 手部区域凸包提取 → 膨胀羽化 → 高斯模糊背景
- 仅手部保持清晰，其余画面模糊处理，类似相机大光圈效果
- 视觉焦点自然集中，演示体验更专业

### 4. 画中画窗口
- 窗口缩小至 120×80（可调），默认固定在屏幕右上角
- `WND_PROP_TOPMOST` 始终置顶，不遮挡 PPT 内容
- 自动获取屏幕分辨率，智能计算锚点位置

---

## 二、PPT 控制与交互

### 5. 直接 COM 操控 PowerPoint
- 通过 **Windows COM 接口** 直接操控 PowerPoint 对象模型，不模拟键盘
- `GetActiveObject` 自动连接已运行的 PPT 实例
- `SlideShowSettings.Run()` 启动放映
- `View.Next()` / `View.Previous()` 翻页，无视窗口焦点
- 无需担心摄像头窗口抢焦点导致按键失效

### 6. 多重防误触机制
- **冷却间隔**：1.2 秒内不会重复触发同一动作
- **帧确认**：手势需连续保持（放映 8 帧 / 翻页 5 帧）才生效
- **状态复位**：手势切换或手离开画面时计数器立即清零

### 7. 视觉反馈系统
- 每种手势对应独立色彩：粉色（五指）、青绿（比1）、金色（比2）
- 动作触发时全屏闪烁 + 大字动画提示
- 底部进度条显示手势保持进度
- 实时 FPS 与幻灯片编号显示

---

## 三、工程设计与可用性

### 8. 模块化架构
```
GestureDetector   →  手势分类 + 帧确认
VisualOverlay     →  霓虹骨架 + HUD + 粒子拖尾 + 闪光动画
PPTController     →  COM 直连 PowerPoint + 冷却控制
blur_background   →  景深虚化 + 凸包蒙版
```

### 9. 开箱即用
- 首次运行自动下载模型文件（7.8MB），缓存至 `C:/temp/`
- 自动处理中文路径兼容问题（MediaPipe C 库限制）
- 仅需 `pip install` 4 个依赖即可运行

### 10. 配置灵活
所有关键参数集中在文件顶部常量区，一目了然：
```python
FIST_HOLD_FRAMES = 8      # 手势确认帧数
COOLDOWN_SEC = 1.2        # 冷却时间
WINDOW_POSITION = "top-right"  # 窗口位置
WINDOW_W, WINDOW_H = 120, 80   # PIP 尺寸
```

---

## 技术栈

| 组件 | 技术 | 用途 |
|------|------|------|
| 手势识别 | MediaPipe Tasks | 21 点手部关键点实时检测 |
| 图像处理 | OpenCV | 摄像头采集、滤镜、渲染 |
| PPT 控制 | pywin32 COM | 直接操控 PowerPoint |
| 数值计算 | NumPy | 凸包、蒙版、动画插值 |
