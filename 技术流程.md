## 技术流程

```
摄像头采集 ──→ MediaPipe 手部检测 ──→ 手势分类 ──→ 帧确认 ──→ COM 控制 PPT
    │                │                    │            │            │
    ▼                ▼                    ▼            ▼            ▼
 OpenCV         21点关键点           五指/比1/比2    防误触机制   下一页/上一页
 帧读取         归一化坐标           指尖vs关节      连续N帧       /开始放映
    │                │                    │            │            │
    └────────────────┴────────────────────┴────────────┘            │
                                    │                              │
                                    ▼                              │
                            景深虚化 + HUD渲染 ←────────────────────┘
                                    │
                                    ▼
                              角落 PIP 窗口显示
```

---

## 各环节详解

### 1. 摄像头采集
- OpenCV `VideoCapture(0)` 打开默认摄像头
- 每帧 `cap.read()` 获取 BGR 图像
- `cv2.flip()` 水平镜像，符合自拍直觉

### 2. MediaPipe 手部关键点检测
- BGR → RGB 色彩空间转换
- 封装为 `mp.Image` 传入 `HandLandmarker`
- `detect_for_video()` 返回 21 个归一化关键点 (x, y, z)
- 关键点编号：0=手腕, 1-4=拇指, 5-8=食指, 9-12=中指, 13-16=无名指, 17-20=小指

### 3. 手势分类逻辑
| 手势 | 判定条件 |
|------|----------|
| 五指张开 | 3+ 手指 TIP.y < PIP.y + 拇指外展 |
| 比 1 | 仅食指伸直，其余 3 指弯曲 |
| 比 2 | 食指+中指伸直，无名指+小指弯曲 |

指尖高于 PIP 关节 = y 值更小 = 手指伸直

### 4. 帧确认防误触
- 同一手势连续出现 N 帧才触发（放映 8 帧 / 翻页 5 帧）
- 手势切换或手离开画面 → 计数器立即归零
- 触发后 1.2 秒冷却，避免连续误触

### 5. COM 控制 PPT
- `GetActiveObject("PowerPoint.Application")` 连接 PPT 实例
- `SlideShowSettings.Run()` 启动全屏放映
- `View.Next()` / `View.Previous()` 翻页
- 直接操作对象模型，不依赖键盘焦点

### 6. 景深虚化
- 21 点关键点 → 凸包 → 膨胀 35px → 高斯羽化蒙版
- `frame * mask + blurred * (1 - mask)` 合成
- 手部清晰 + 背景模糊 = 大光圈效果

### 7. PIP 窗口渲染
- 窗口缩小至 120×80，置顶固定在屏幕右上角
- 叠加霓虹骨架 + HUD 面板 + 粒子拖尾 + 动作闪屏
- `cv2.imshow()` 输出最终画面
